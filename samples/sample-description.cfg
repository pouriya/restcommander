config.toml = Sample configuration
perl.pl = Sample Perl script
python.py = Sample Python(v3) script
self-signed-cert.pem = A self-signed SSL certificate (ONLY FOR TEST PURPOSES)
self-signed-key.pem = A self-signed SSL private key (ONLY FOR TEST PURPOSES)
shell.sh = Sample Shell script
systemd.service = Sample Systemd configuration
test-script = A script to test service HTTP API status codes and body
test-script-info.yml = YAML information for test script
